const express = require('express');
const router = express.Router();
// TODO: Import proposal controller functions
// const {
//   createProposal,
//   getProposalById,
//   listProposals,
//   updateProposal,
//   deleteProposal,
//   voteOnProposal,
//   getProposalVotes,
//   getProposalResults
// } = require('../controllers/proposalController');

// All routes require authentication (applied in app.js)

// POST /api/proposals - Create new proposal
// router.post('/', createProposal);

// GET /api/proposals - List proposals
// router.get('/', listProposals);

// GET /api/proposals/:id - Get proposal details
// router.get('/:id', getProposalById);

// PUT /api/proposals/:id - Update proposal
// router.put('/:id', updateProposal);

// DELETE /api/proposals/:id - Delete proposal
// router.delete('/:id', deleteProposal);

// POST /api/proposals/:id/vote - Vote on proposal
// router.post('/:id/vote', voteOnProposal);

// GET /api/proposals/:id/votes - Get proposal votes
// router.get('/:id/votes', getProposalVotes);

// GET /api/proposals/:id/results - Get proposal results
// router.get('/:id/results', getProposalResults);

module.exports = router;

